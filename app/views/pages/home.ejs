<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Calidad de Software' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/styles/styles.css" class="">
</head>
<body>
    <!-- Navigation Menu -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Calidad de Software</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/">Inicio</a>
                    </li>
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/profile">Mi Perfil</a>
                        </li>
                        <!-- Admin puede ver todo -->
                        <% if (user.roles && user.roles.includes('ROLE_ADMIN')) { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/admin-dashboard">Admin</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/moderator-dashboard">Subir Banner</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/user-dashboard">Subir Noticia</a>
                            </li>
                        <% } else if (user.roles && user.roles.includes('ROLE_MODERATOR')) { %>
                            <!-- Moderator ve moderator y user -->
                            <li class="nav-item">
                                <a class="nav-link" href="/moderator-dashboard">Subir Banner</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/user-dashboard">Subir Noticia</a>
                            </li>
                        <% } else { %>
                            <!-- User normal solo ve user dashboard -->
                            <li class="nav-item">
                                <a class="nav-link" href="/user-dashboard">Subir Noticia</a>
                            </li>
                        <% } %>
                        <li class="nav-item">
                            <a class="nav-link" href="/logout">Cerrar Sesión</a>
                        </li>
                    <% } else { %>
                        <!-- Usuario no logueado -->
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Iniciar Sesión</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">Registrarse</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Banner Slider Dinámico -->
    <div id="bannerCarousel" class="carousel slide" data-bs-ride="carousel">
        <% if (activeBanners && activeBanners.length > 0) { %>
            <!-- Indicadores dinámicos -->
            <div class="carousel-indicators">
                <% activeBanners.forEach((banner, index) => { %>
                    <button type="button" data-bs-target="#bannerCarousel" 
                            data-bs-slide-to="<%= index %>" 
                            class="<%= index === 0 ? 'active' : '' %>"
                            aria-label="Banner <%= index + 1 %>"></button>
                <% }); %>
            </div>

            <!-- Banners dinámicos -->
            <div class="carousel-inner">
                <% activeBanners.forEach((banner, index) => { %>
                    <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                        <% if (banner.Enlace) { %>
                            <a href="<%= banner.Enlace %>" target="_blank" style="display: block; height: 100%;">
                        <% } %>
                            <img src="/banners/<%= banner.Imagen %>" 
                                 class="d-block w-100" 
                                 alt="<%= banner.Titulo %>"
                                 onerror="this.src='data:image/svg+xml;base64,'">
                        <% if (banner.Enlace) { %>
                            </a>
                        <% } %>
                        
                        <div class="carousel-caption">
                            <h3><%= banner.Titulo %></h3>
                            <% if (banner.Descripción) { %>
                                <p><%= banner.Descripción %></p>
                            <% } %>
                            <% if (!user && !banner.Enlace) { %>
                                <a href="/register" class="btn btn-primary mt-3">Únete a nuestra comunidad</a>
                            <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>

            <!-- Controles de navegación -->
            <% if (activeBanners.length > 1) { %>
                <button class="carousel-control-prev" type="button" data-bs-target="#bannerCarousel" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Anterior</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#bannerCarousel" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Siguiente</span>
                </button>
            <% } %>

        <% } else { %>
            <!-- Banner por defecto cuando no hay banners activos -->
            <div class="carousel-inner">
                <div class="carousel-item active banner-placeholder">
                    <div class="carousel-caption">
                        <h3>Bienvenido <%= user ? user.username : 'Invitado' %></h3>
                        <p><%= user ? 'Gracias por visitarnos' : 'No hay banners activos en este momento.' %></p>
                        <% if (!user) { %>
                            <a href="/register" class="btn btn-primary mt-3">Únete a nuestra comunidad</a>
                        <% } %>
                    </div>
                </div>
            </div>
        <% } %>
    </div>

    <!-- Sección de Noticias Scrapeadas -->
    <% if (activeNews && activeNews.length > 0) { %>
    <section class="scraped-news-section py-5">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="text-center mb-0">Noticias Recientes</h2>
                <% if (user) { %>
                    <a href="/user-dashboard" class="btn btn-outline-primary">
                        <i class="fas fa-plus me-2"></i>Obtener Más Noticias
                    </a>
                <% } %>
            </div>
            <div class="row">
                <% activeNews.forEach((noticia, index) => { %>
                    <div class="col-md-6 col-lg-4 mb-4">
                        <div class="card h-100 news-card">
                            <span class="badge bg-info news-badge"><%= noticia.Fuente %></span>
                            
                            <% if (noticia.Imagen) { %>
                                <img src="<%= noticia.Imagen %>" 
                                     class="card-img-top news-image" 
                                     alt="<%= noticia.Titulo %>"
                                     onerror="this.src='data:image/svg+xml;base64,'">
                            <% } else { %>
                                <div class="card-img-top news-image bg-light d-flex align-items-center justify-content-center">
                                    <i class="fas fa-newspaper fa-3x text-muted"></i>
                                </div>
                            <% } %>
                            
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title"><%= noticia.Titulo %></h5>
                                <p class="card-text flex-grow-1"><%= noticia.Resumen %></p>
                                <div class="mt-auto">
                                    <div class="d-flex justify-content-between align-items-center mb-2">
                                        <small class="text-muted">
                                            <i class="fas fa-user me-1"></i><%= noticia.Autor %>
                                        </small>
                                        <small class="text-muted">
                                            <i class="fas fa-calendar me-1"></i>
                                            <%= noticia.createdAt ? new Date(noticia.createdAt).toLocaleDateString('es-ES') : 'Reciente' %>
                                        </small>
                                    </div>
                                    <% if (noticia.Enlace) { %>
                                        <a href="<%= noticia.Enlace %>" target="_blank" class="btn btn-outline-primary btn-sm w-100">
                                            <i class="fas fa-external-link-alt me-1"></i>Leer noticia completa
                                        </a>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <% if (activeNews.length >= 6 && user) { %>
                <div class="text-center mt-4">
                    <a href="/user-dashboard" class="btn btn-primary">
                        <i class="fas fa-newspaper me-2"></i>Ver Todas las Noticias
                    </a>
                </div>
            <% } %>
        </div>
    </section>
    <% } %>


    <section class="py-5 bg-light-custom">
        <div class="container">
            <h2 class="text-center mb-5">Nuestros Servicios</h2>
            <div class="row text-center">
                <div class="col-md-4 mb-4">
                    <div class="feature-item bg-white">
                        <i class="fas fa-bug fa-3x text-primary mb-3"></i>
                        <h4>Control de Calidad</h4>
                        <p>Garantizamos la excelencia en cada línea de código con pruebas rigurosas y metodologías ágiles de testing.</p>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="feature-item bg-white">
                        <i class="fas fa-code fa-3x text-success mb-3"></i>
                        <h4>Desarrollo Web</h4>
                        <p>Creamos soluciones web personalizadas, escalables y optimizadas para todos los dispositivos.</p>
                    </div>
                </div>
                <div class="col-md-4 mb-4">
                    <div class="feature-item bg-white">
                        <i class="fas fa-chart-line fa-3x text-warning mb-3"></i>
                        <h4>Consultoría Tecnológica</h4>
                        <p>Asesoramos en la transformación digital de tu empresa con las tecnologías más innovadoras.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer class="bg-dark text-white py-4">
        <div class="container text-center">
            <p>&copy; 2024 Calidad de Software. Todos los derechos reservados.</p>
            <div class="social-links mt-2">
                <a href="#" class="text-white me-3"><i class="fab fa-facebook-f"></i></a>
                <a href="#" class="text-white me-3"><i class="fab fa-twitter"></i></a>
                <a href="#" class="text-white me-3"><i class="fab fa-linkedin-in"></i></a>
                <a href="#" class="text-white"><i class="fab fa-github"></i></a>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>