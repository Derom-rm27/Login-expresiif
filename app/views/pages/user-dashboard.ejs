<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/styles/styles.css" class="">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
<nav class="col-md-3 col-lg-2 d-md-block sidebar collapse">
    <div class="position-sticky pt-3">
        <div class="text-center mb-4">
            <h4 class="text-white">
                <i class="fas fa-user-shield me-2"></i>Moderator Panel
            </h4>
        </div>
        <ul class="nav flex-column">
            <% if (user.roles && user.roles.includes('ROLE_ADMIN')) { %>
                <li class="nav-item">
                    <a class="nav-link active" href="/admin-dashboard">
                        <i class="fas fa-images me-2"></i>Gestión de Users
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/visit-report">
                        <i class="fas fa-chart-bar me-2"></i>Reporte de Visitas
                    </a>
                </li>
            <% } else if ((user.roles && user.roles.includes('ROLE_MODERATOR')) || (user.roles && user.roles.includes('ROLE_ADMIN'))) { %>
                <li class="nav-item">
                    <a class="nav-link active" href="/admin-dashboard">
                        <i class="fas fa-images me-2"></i>Gestión de Banners
                    </a>
                </li>
            <% } else if ((user.roles && user.roles.includes('ROLE_USER')) || (user.roles && user.roles.includes('ROLE_MODERATOR')) || (user.roles && user.roles.includes('ROLE_ADMIN'))) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/profile">
                        <i class="fas fa-user me-2"></i>Mi Perfil
                    </a>
                </li>
            <% } %>
            
            <li class="nav-item">
                <a class="nav-link" href="/">
                    <i class="fas fa-home me-2"></i>Inicio
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/logout" onclick="return confirm('¿Cerrar sesión?')">
                    <i class="fas fa-sign-out-alt me-2"></i>Cerrar Sesión
                </a>
            </li>
        </ul>
    </div>
</nav>

            <!-- Main content -->
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2">
                        <i class="fas fa-newspaper me-2"></i>Gestión de Noticias
                    </h1>
                </div>

                <!-- Mensajes -->
                <% if (typeof message !== 'undefined' && message) { %>
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <%= message %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                <% } %>
                
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <%= error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                <% } %>

                <!-- Formulario para scraping -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="fas fa-download me-2"></i>Obtener Noticias
                        </h5>
                    </div>
                    <div class="card-body">
                        <form action="/scrape-news" method="POST">
                            <div class="row">
                                <div class="col-md-8">
                                    <div class="mb-3">
                                        <label class="form-label">URL del Sitio de Noticias *</label>
                                        <input type="url" class="form-control" name="newsUrl" 
                                               placeholder="https://ejemplo.com/noticias" required>
                                        <div class="form-text">
                                            Ingresa la URL de un sitio de noticias para extraer contenido automáticamente.
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="mb-3">
                                        <label class="form-label">&nbsp;</label>
                                        <button type="submit" class="btn btn-primary w-100">
                                            <i class="fas fa-search me-2"></i>Buscar Noticias
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- Lista de noticias -->
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">
                            <i class="fas fa-list me-2"></i>Noticias Obtenidas
                        </h5>
                    </div>
                    <div class="card-body">
                        <% if (news && news.length > 0) { %>
                            <div class="row">
                                <% news.forEach(noticia => { %>
                                    <div class="col-md-6 col-lg-4 mb-4">
                                        <div class="card news-card">
                                            <% if (noticia.Imagen) { %>
                                                <img src="<%= noticia.Imagen %>" 
                                                     class="card-img-top news-image" 
                                                     alt="<%= noticia.Titulo %>"
                                                     onerror="this.src='data:image/svg+xml;base64,'">
                                            <% } %>
                                            <div class="card-body">
                                                <h6 class="card-title"><%= noticia.Titulo %></h6>
                                                <p class="card-text small text-muted"><%= noticia.Resumen %></p>
                                                <div class="d-flex justify-content-between align-items-center">
                                                    <small class="text-muted">
                                                        Por: <%= noticia.Autor %>
                                                    </small>
                                                    <span class="badge bg-info">
                                                        <%= noticia.Fuente %>
                                                    </span>
                                                </div>
                                            </div>
                                            <div class="card-footer">
                                                <div class="btn-group w-100">
                                                    <% if (noticia.Enlace) { %>
                                                        <a href="<%= noticia.Enlace %>" target="_blank" class="btn btn-sm btn-primary">
                                                            <i class="fas fa-external-link-alt"></i> Ver Original
                                                        </a>
                                                    <% } %>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                        <% } else { %>
                            <div class="text-center py-4">
                                <i class="fas fa-newspaper fa-3x text-muted mb-3"></i>
                                <h5 class="text-muted">No hay noticias obtenidas</h5>
                                <p class="text-muted">Usa el formulario superior para obtener noticias de sitios web.</p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>